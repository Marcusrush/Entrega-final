1 crear HTML + contenido
2 vincular carpetas e imagenes
3 Crar carpeta css + archivo css
4 Crear carpeta scss + archivo scss
iniciar scss:
 A npm init - gestor de paquetes de NODE
 B npm install -g sass
 C sass --watch scss:css
 ctrl + c se detiene el watch
 5 crear carpeta scss
  + styles.scss + _variables.scss
  En stlyes poner @import "variables" para que importe a styles.scss
  crear variables de: colores, fuentes, resoluciones
  6 crear _comunes.scss
 
 Trabajar con git:

1 Git init
Al iniciar git, una sola vez

2 Git status
Para chequear estado del git

3 git add .
Esto permite agregar las modificaciones que hayamos hecho.

4 git commit -m
 First commit

5 Ramas

$ git branch
* main (Master)


$ git checkout mi-rama / para cambiar de main a la rama creada. 
$ git checkout main / mi-rama

7 git branch - borra rama
